{
	"name": "ceiling-the-game",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "deno run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
	}
}